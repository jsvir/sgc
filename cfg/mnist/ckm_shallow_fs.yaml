base_cfg: cfg/mnist/common.yaml
clustering: CKMFS

stg:
  sigma: 0.5
  reg_lamba: .1
  hidden_dim: 64

ckm:
  reg_lamba: 1e-2
  centroid_dim: 10

selector:
  k_selected: 10
  hidden_dim: 128
  start_temp: 10
  min_temp: 1e-2

